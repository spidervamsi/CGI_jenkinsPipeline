pipeline { 
    agent any  
     tools {
       maven 'LOCALMAVEN'
   }
   stages {
       stage('Build') {
           steps {
               
               echo "scm completed boss"
           }
       }
   }
}
